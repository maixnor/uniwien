# Schreiben Sie die Antwort für jede der angegebenen
# Fragen auf die Zeile nach der jeweiligen Frage, die
# nicht mit einem "#" Symbol beginnt. Schreiben Sie
# die ganze Antwort in eine Zeile.
#
# Wie heißt die C-Konstante fuer das Protokoll aus der Internet Protocol-Implementierung (vorkommend im IP-Header), die als Default-Wert beim Erstellen eines Sockets angenommen wird mit domain-Parameter AF_INET, type-Parameter SOCK_STREAM und protocol-Parameter 0?

